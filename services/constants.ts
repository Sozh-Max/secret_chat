export const enum LOCAL_STORAGE_KEYS {
	IS_INIT = 'isInit',
	USER_DATA = 'userData',
	DIALOGS = 'dialogs',
	TOKENS = 'tokens',
	GOOGLE_REFERRER = 'googleReferrer',
	UID = 'uid',
}

export const enum PLATFORM {
	IOS = 'ios',
	ANDROID = 'android',
}