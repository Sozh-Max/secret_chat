import { StyleSheet } from 'react-native';
import { TEXT_BOLD } from '@/src/constants/global';

export const styles = StyleSheet.create({
  text: {
    fontFamily: TEXT_BOLD,
    color: 'white',
    fontSize: 14,
    lineHeight: 18,
    textAlign: 'right',
  },
});