import { StyleSheet } from 'react-native';
import { TEXT_BOLD } from '@/constants/global';

export const styles = StyleSheet.create({
  text: {
    fontFamily: TEXT_BOLD,
    color: 'white',
    fontSize: 16,
    lineHeight: 20,
    textAlign: 'right',
  },
});