import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'column',
    height: '100%',
  },
  container: {
    padding: 16,
    flexGrow: 1,
  },
  inner: {
    paddingBottom: 30,
  },
});